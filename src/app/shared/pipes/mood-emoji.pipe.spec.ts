import { MoodEmojiPipe } from './mood-emoji.pipe';

describe('MoodEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new MoodEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
