import { MoodMessageDirective } from './mood-message.directive';

describe('MoodMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new MoodMessageDirective();
    expect(directive).toBeTruthy();
  });
});
